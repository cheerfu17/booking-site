import React, { useContext, useEffect } from 'react';
import { getHalls } from '../components/http/bookingAPI';
import { Context } from '../index';

const Halls = () => {

    return (
        <div>
            HALLS
        </div>
    );
};

export default Halls;